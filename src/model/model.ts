export interface ICardNew {
    id: string,
    img: HTMLImageElement,
    title: string,
    description: string
}

export interface IAboutCard {
    id: string,
    img: any,
    title: string,
    description: string
}